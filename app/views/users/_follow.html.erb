<%= form_for(current_user.relationship.build(followed_id: @user.id),
             method: create, remote:true) do |f| %>
    <div>
      <%= f.hidden_field :followed_id %>
    </div>
    <%= f.submit 'Follow', class: 'btn btn-large btn-primary' %>
<% end %>